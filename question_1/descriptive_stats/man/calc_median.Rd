% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_median.R
\name{calc_median}
\alias{calc_median}
\title{Calculate the Median}
\usage{
calc_median(x, na.rm = TRUE)
}
\arguments{
\item{x}{A numeric vector.}

\item{na.rm}{Logical. Should \code{NA} values be removed before computation?
Defaults to \code{TRUE}.}
}
\value{
A single numeric value: the median of \code{x}. Returns \code{NA_real_}
if \code{x} is empty (or becomes empty after \code{NA} removal).
}
\description{
Computes the median (middle value) of a numeric vector. For vectors of
even length, the median is the average of the two middle values.
}
\examples{
calc_median(c(1, 2, 2, 3, 4, 5, 5, 5, 6, 10))
# 4.5

# Odd-length vector
calc_median(c(1, 3, 5))
# 3

# Handles NA values
calc_median(c(1, 2, NA, 4, 5))
# 3

}
